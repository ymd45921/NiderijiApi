{
  "name": "nideriji-api",
  "version": "0.1.0",
  "description": "API of Nideriji and Ohshenghuo.",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "author": "Nanami",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "dependencies": {
    "axios": "^0.26.1"
  },
  "scripts": {
    "build": "rimraf ./build && tsc -d --outDir build",
    "clear": "rimraf ./build && rimraf ./dist",
    "build:to": "rimraf ./build && tsc -d --outDir",
    "build:lib": "rimraf ./dist && tsc -d",
    "build:declare": "rimraf ./dist && tsc --outdir dist --emitDeclarationOnly"
  },
  "devDependencies": {
    "@types/node": "^17.0.25",
    "dotenv": "^16.0.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.6.3"
  }
}
